# Public origin used in Copy Link, Share Room, and QR codes.
# Optional fixed public origin. Leave empty to use the incoming request origin.
PUBLIC_GAME_URL=
# Local default; hosting platforms may provide their own PORT.
PORT=7000
