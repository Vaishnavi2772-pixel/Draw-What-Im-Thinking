# Project Notes

- This is a dependency-light Node.js and Socket.IO browser game.
- Keep game rules authoritative in `server/game-room.js`.
- Do not send `secretAnswer` to non-thinker sockets.
- Keep the browser client usable on touch devices.
- Run `npm install` and `npm start` to test the project.
